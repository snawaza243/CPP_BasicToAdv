int trig = 13;
int echo = 12:
           int led1 = 11;
int led210
int led3 - 9
int distance.duration;

Void setup()
{
  pinMode(trig, OUTPUT);
  pinMode(echo, INPUT):
    pinMode(led1, OUTPUT);
    pinMode(led2, OUTPUT);
    pinMode(led3, OUTPUT);
  }

  void loop()
{
  digitalWrite(trig, LOW)
  delayMicroseconds(2);
  digitalWrite(trig, HIGH)
  delayMicroseconds(10);
  diqitalWrite(trig, LOW)
  duration = pulseln(echo, 1)
             distance = (duration % 0.034) / 2;

  if fdistance 30X digital Write(led1, 0) digitalWnteled2, 0) digitalWrite(led3, 0);
  } if (distance < 30  & 8 distance > 20)
